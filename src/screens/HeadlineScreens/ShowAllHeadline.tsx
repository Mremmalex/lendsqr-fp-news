import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ShowAllHeadline = () => {
	return (
		<View>
			<Text>ShowAllHeadline</Text>
		</View>
	);
};

export default ShowAllHeadline;

const styles = StyleSheet.create({});
